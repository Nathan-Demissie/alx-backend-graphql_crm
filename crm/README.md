# CRM Report Setup with Celery

## Install Redis and dependencies
- Install Redis: `sudo apt install redis`
- Install Python packages: `pip install -r requirements.txt`

## Run migrations
```bash
python manage.py migrate
